global:
  scrape_interval: 1s

scrape_configs:
  - job_name: "gateway"
    static_configs:
      - targets: ["localhost:2112"]

  - job_name: "worker"
    static_configs:
      - targets: ["localhost:2113"]